package Prog2;

/**
 * Clase : Representacion de una idea y su rol principal es crear un objetos(instancia).
 *         Toda clase tiene siempre un único padre.
 *
 *         Mensajes: Que es lo quye tienen que hacer los objetos.
 *         Métodos: Como resolver lo que pide el mensaje.
 *
 *                  - Estaticamente tipado: En tiempo de comp el tipo (int, otros) tiene que poder ser deducible.
 *
 *  Polimorfismo:
 *          - Es una relación entre 2 o mas objetos con respecto a un conjunto de mensajes (que sabe hacer) semandticamente iguales.
 *
 * --------------------------------------------------------------------------------------------------------------------
 *
 *   no usar el if ():
 *          -
 *
 */
